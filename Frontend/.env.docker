# The React app runs in the user's BROWSER, which reaches the backend through the
# host port mapping (3000:3000) — NOT through the compose service name.
# So this must be localhost, never http://backend:3000.
VITE_REST_SERVER_URL=http://localhost:3000
